name: Update CSV via MinIO

on:
  schedule:
    - cron: '15 4 * * 1-5'  # 9:45 AM IST (Mon–Fri)
  workflow_dispatch:

jobs:
  update:
    runs-on: self-hosted

    steps:
      - name: Checkout `reporting` branch
        uses: actions/checkout@v4
        with:
          ref: reporting
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install boto3 pandas matplotlib openpyxl

      - name: Run update_csv.py to generate CSVs
        run: python scripts/update_csv.py

      - name: Remove old CSVs
        run: |
          rm -f minio-report-tracker/spreadsheet/*.csv
          git rm -f minio-report-tracker/spreadsheet/*.csv || echo "No old CSVs to remove"

      - name: Move new CSVs into Git folder
        run: |
          mkdir -p minio-report-tracker/spreadsheet
          mv ../spreadsheet/*.csv minio-report-tracker/spreadsheet/

      - name: Run generate_xlsx.py to generate Excel reports
        run: python scripts/generate_xlsx.py

      - name: Remove old XLSX files
        run: |
          rm -f minio-report-tracker/xlxs/*.xlsx
          git rm -f minio-report-tracker/xlxs/*.xlsx || echo "No old XLSX files to remove"

      - name: Move new XLSX files into Git folder
        run: |
          mkdir -p minio-report-tracker/xlxs
          mv ../xlxs/*.xlsx minio-report-tracker/xlxs/

      - name: Commit and Push Changes to `reporting` branch
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add minio-report-tracker/spreadsheet/*.csv minio-report-tracker/xlxs/*.xlsx
          git commit -m "📊 Updated CSV and XLSX reports for all environments" --allow-empty || echo "✅ No changes to commit"
          git push origin reporting
